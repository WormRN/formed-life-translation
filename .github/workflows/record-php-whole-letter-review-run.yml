name: Record Philippians whole-letter review run (archived)
run-name: Confirm archived Philippians whole-letter receipt

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    name: Archived — no writes and zero provider calls
    runs-on: ubuntu-latest
    steps:
      - name: Report stable receipt
        run: |
          echo "Stable review receipt: config/latest_php_whole_letter_review_run.json"
          echo "Completed run: 30795801547"
          echo "This locator is frozen and cannot write to main."
